# Optional fallback (browser uses relative /digitalhouse/backend — see vite proxy).
# Local dev: .env.development sets http://localhost:4000 automatically.
# VITE_API_BASE=http://localhost:4000
# Admin API key (same as backend ADMIN_API_KEY) – required for X-Admin-Key
VITE_ADMIN_KEY=your-admin-key-here
