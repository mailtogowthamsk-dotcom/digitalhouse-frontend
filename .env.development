# Used by `npm run dev` only (overrides .env).
# Relative path → Vite proxy → http://localhost:4000 (same as before).
# Start backend: cd backend && npm run dev
# VITE_API_BASE=/digitalhouse/backend
# VITE_API_BASE=https://www.infosensetechnologies.com/digitalhouse/backend
VITE_API_BASE=http://192.168.160.183:4000